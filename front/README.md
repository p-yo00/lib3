# Java Spring Boot Web Project

이 프로젝트는 Gradle 기반의 Java Spring Boot 웹 프로젝트입니다.

## 실행 방법

1. Gradle 빌드: `./gradlew build` (Windows: `gradlew.bat build`)
2. 애플리케이션 실행: `./gradlew bootRun` (Windows: `gradlew.bat bootRun`)

## 주요 기능
- REST API 엔드포인트 예시 포함

---

프로젝트 구조 및 추가 개발 방법은 README를 참고하세요.
